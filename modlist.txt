# Default launch arguments for this install. The engine reads this file from the
# game folder at startup and injects its tokens AHEAD of any real command-line
# args, so no .bat is needed and one install can play every mod here.
# '#' or ';' starts a comment; the rest of that line is ignored.
#
# ---- WHICH MOD DOES THIS INSTALL RUN?  Uncomment ONE line below. ----
#
#   Base Tribes .......... leave all of them commented
#   Tribes RPG / Kronos .. -mod rpg
#   Red Moon RPG ......... -mod rpg -mod rmrpg    <- BOTH: RMRPG stacks on rpg
#
# This applies to playing AND to hosting, so choose the mod here, then launch
# the game normally, or host a dedicated server with:
#
#   InfiniteSpawn.exe *ModernTribes.exe -dedicated
#
-mod rpg
# -mod rpg -mod rmrpg
